# Active Task

<!-- BEGIN MMM TASKCTL EXECUTION VIEW -->
<!-- Generated by mmm.execution.taskctl; do not edit. -->
<!-- END MMM TASKCTL EXECUTION VIEW -->

**Owner:** MMM repository governance
**Last updated:** 2026-09-18
**Last verified:** 2026-09-18

## Identity and supersession prerequisite

- **Task ID:** `MMM_REPOSITORY_HANDOFF_ACTIVE_TASK_CONTRACT_ASSERTIONS_001`
- **Repository:** `Phani-Pavuluri/MMM`
- **Synchronized base:** `54b9bc43656bbdbc09c6ad63df43098720ffe921`
- **Declared future feature branch:** `feat/mmm-repository-handoff-active-task-contract-assertions-001`
- **Execution mode:** `branch_and_fast_forward`
- **Risk tier:** Tier 1 MMM repository-governance regression-test repair with mandatory full repository validation
- **Compatibility policy:** `not_applicable` for public, package, schema, data, runtime, analytical, product, consumer, MIP, and GeoX behavior

This is an independent successor to the blocked taskctl-fixture repair at remote
head `f3cb51f61179ca939968036ae07289ae8533035d`. That branch proved the fixture
repair itself, but its mandatory Docker gate found four assertions in
`tests/test_repo_native_execution_handoff.py` that require superseded prose from
the prior active task. Preserve that branch, its implementation
`0a7f6e796c824d1dab007291c25dee900f765b95`, its blocked receipt, and the
separately blocked Finder-hygiene branch unchanged.

Before authorization, re-bootstrap a clean isolated worktree from synchronized
`origin/main`, verify this task's main authorization and exact remote branch
identity/ancestry, and verify both preserved blocked receipts. Any disagreement
in repository, task, branch, authorization ancestry, blocked-receipt, or owned
path evidence is fail-closed: do not broaden scope; publish `blocked` only on a
safe authorized branch with the exact mismatch, validation categories, and a
live resolution condition.

## Primary independently reviewable outcome

Make `tests/test_repo_native_execution_handoff.py` validate stable execution
standards and the taskctl state contract rather than prose that belongs only to
a particular active task. The four stale assertions must pass for the current
Git-authored task contract while retaining coverage for the durable governance
requirements they were intended to prove.

## Atomic boundary

The four assertions represent one defect: a repository-wide semantic test
incorrectly treats transient task prose as a stable protocol contract. Repairing
only some assertions leaves the full gate blocked; changing standards or taskctl
would widen the fix beyond the failed oracle. This task therefore changes one
test module plus its execution evidence only.

## Why this task cannot be split further

The assertions share one test module, one stale-contract cause, and one full-gate
failure. A partial repair cannot establish that a newly authored active task is
compatible with the semantic test suite, and splitting the individual assertions
would create several review units with no independently useful outcome.

## Inputs, outputs, invariants, and failure semantics

- **Inputs:** synchronized main; blocked receipts `f3cb51f61179ca939968036ae07289ae8533035d` and `edb4fb10c1870edb468a72cd09ef91b9ae6674e3`; `tests/test_repo_native_execution_handoff.py`; the current taskctl state/view contract; a clean isolated worktree.
- **Outputs:** the four assertions validate stable AGENTS/standard/state/taskctl-view facts and no longer require task-specific headings, wording, or schema literals in `ACTIVE_TASK.md`.
- **Invariants:** `AGENTS.md`, `mmm/execution/taskctl.py`, lifecycle transitions, state schema, authority controls, required validation, and all non-test behavior remain unchanged; the test retains durable checks for lean delivery, fail-closed authority, exact remote-head/closure controls, and taskctl-state identity.
- **Failure semantics:** if the assertions cannot be made stable without weakening their intended governance coverage, any other test fails, validation fails, a prohibited path changes, or a preserved receipt conflicts, do not alter standards or taskctl. Publish a Git-durable `blocked` state with exact evidence and a live resolution condition.

## Exact implementation behavior

1. Replace the two assertions that require active-task prose with assertions over the stable execution standards and/or the generated taskctl view/state identity.
2. Replace the stale `exact-head` string oracle with the policy's actual exact remote feature-branch SHA requirement, without weakening merge/closure coverage.
3. Assert the `mmm_repo_execution_state_v3` schema where it is authoritative (`EXECUTION_STATE.json`) and retain a taskctl-view assertion for `ACTIVE_TASK.md`; do not require a schema literal in task prose.
4. Add no task-specific prose requirements to the regression test and change no files beyond the owned paths.

## Owned paths

1. `tests/test_repo_native_execution_handoff.py`
2. `docs/execution/ACTIVE_TASK.md`
3. `docs/execution/EXECUTION_STATE.json`
4. `docs/execution/LATEST_COMPLETION_REPORT.md`

No other path is owned.

## Prohibited scope

Do not change `.DS_Store`, `.gitignore`, Finder-hygiene files, `AGENTS.md`,
`mmm/execution/taskctl.py`, production source, fixtures, packages, public APIs,
schemas, contracts, configuration, dependencies, CI, releases, deployment,
documentation outside the three execution files, MIP, GeoX, analytical, public,
product, package, runtime, or consumer behavior. Do not change capability
authority. Do not create a PR, merge, squash, rebase, force-push, merge commit,
or pre-merge approval commit.

## Acceptance evidence and validation

On the frozen exact task-owned tree record the four repaired assertions and the
full handoff semantic-test result; source proof that no active-task-specific
oracle remains; changed-path proof limited to owned paths; JSON/taskctl/diff
checks; remote feature-head equality; and Docker-backed `make validate`.

Required validation: `python -m json.tool docs/execution/EXECUTION_STATE.json
>/dev/null`; `python3 -m pytest tests/test_repo_native_execution_handoff.py`;
`poetry run python -m mmm.execution.taskctl check`; `git diff --check`; and
`make validate`. `make validate` is mandatory. Separate Ruff and mypy
categories are `not_required` because only a repository-governance test changes,
but are not waived from `make validate`. A required category that cannot run is
`blocked`.

## Authorization, publication, and stop conditions

This is proposed authoring only: no feature branch, execution, implementation,
correction, PR, merge, MIP, GeoX, analytical, sibling, or capability authority.
Only a future main-only `proposed -> authorized` transition may create the
declared branch and begin execution.

Future execution creates one implementation and one exact-tree receipt commit.
The receipt records the four repaired-oracle results, stable-contract coverage,
required validation categories, changed paths, exact remote feature head,
blockers/debt, and unchanged authority. Publish only `ready_for_review` with
correction, merge, and PR authority false, no blockers, null reviewed/approval
SHAs, and remote-head equality. A genuine failure publishes `blocked` on the
safe authorized branch.

## Deferred successors

- Resume the blocked taskctl-fixture cleanup only after this task is merged and its full gate is green.
- Resume the preserved Finder-hygiene task only after the fixture cleanup merges and its full gate reruns under its own authority.
- Generalize unrelated execution tests only after a distinct demonstrated stale-contract defect.
- Production taskctl, lifecycle, authority, MIP, GeoX, analytical, public, product, or package change.

**Unresolved execution-blocking design questions: none.**
