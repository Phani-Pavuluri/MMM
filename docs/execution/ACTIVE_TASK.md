# Active Task

<!-- BEGIN MMM TASKCTL EXECUTION VIEW -->
<!-- Generated by mmm.execution.taskctl; do not edit. -->
- Task ID: `MMM_DOCKER_VALIDATION_EVIDENCE_CAPTURE_001`
- Repository: `Phani-Pavuluri/MMM`
- Status: `authorized`
- Decision: `authorized`
- Execution mode: `branch_and_fast_forward`
- Base SHA: `3ce50ca4ef48b9acfa72a60cee877e78b40b3b12`
- Task authoring SHA: `c89dbef98645130d947ad1a267c2bc6263f7a5fc`
- Authorization SHA: `c89dbef98645130d947ad1a267c2bc6263f7a5fc`
- Feature branch: `feat/mmm-docker-validation-evidence-capture-001`
- Feature branch created: `true`
- Task execution authorized: `true`
- Correction execution authorized: `false`
- Merge authorized: `false`
- PR creation authorized: `false`
- Implementation SHA: `null`
- Reviewed head SHA: `null`
- Rejected review head SHA: `null`
- Rejected implementation SHA: `null`
- Approval SHA: `null`
- Blockers: `[]`
- Live resolution condition: `null`
- Correction cycles completed: `0`
- Correction cycles remaining: `1`
- Local feature branch cleanup: `not_required`
- Remote feature branch cleanup: `not_required`
- Analytical authority changed: `false`
- Sibling authority changed: `false`
- Capability authorizations changed: `false`
<!-- END MMM TASKCTL EXECUTION VIEW -->

**Owner:** MMM repository governance
**Last updated:** 2026-09-18
**Last verified:** 2026-09-18

## Identity and supersession prerequisite

- **Task ID:** `MMM_DOCKER_VALIDATION_EVIDENCE_CAPTURE_001`
- **Repository:** `Phani-Pavuluri/MMM`
- **Synchronized base:** `3ce50ca4ef48b9acfa72a60cee877e78b40b3b12`
- **Declared future feature branch:** `feat/mmm-docker-validation-evidence-capture-001`
- **Execution mode:** `branch_and_fast_forward`
- **Risk tier:** Tier 1 MMM repository-governance validation-evidence recovery with mandatory full Docker validation
- **Compatibility policy:** `not_applicable` for public, package, schema, data, runtime, analytical, product, consumer, MIP, and GeoX behavior

This is an independent successor to the blocked assertion-repair receipt at
`3022498ba0e15d810bc373fcf3ad95a288cc3908`. That receipt identifies a one-file
candidate implementation, `a26f4498e78be61bf749d8206c457cd8c921e917`, whose
focused proof passed but whose required Docker gate exited `1` without retained
failure output. Preserve that receipt and the earlier taskctl-fixture and
Finder-hygiene blocked receipts unchanged.

Before execution, re-bootstrap a clean isolated worktree, verify main identity,
this authorization ancestry, and all three blocked receipts. Any mismatch is
fail-closed: publish `blocked` only on this safe branch with exact evidence and
a live resolution condition.

## Primary independently reviewable outcome

Reapply exactly the candidate assertion-test diff from `a26f449` and run the
repository-required `make validate` once with host-side tee capture. Publish the
full gate result and retained final failure evidence, or a ready-for-review
receipt if the exact tree passes.

## Atomic boundary

The assertion repair and observable rerun are inseparable: without the repair,
the known four stale oracles fail first; without retained output, a later gate
failure cannot be safely assigned. This task changes only that test and execution
evidence; it does not alter the validation wrapper or test behavior beyond the
already-proposed stable-oracle diff.

## Inputs, outputs, invariants, and failure semantics

- **Inputs:** synchronized main; candidate commit `a26f4498e78be61bf749d8206c457cd8c921e917`; blocked receipts; `tests/test_repo_native_execution_handoff.py`; Docker; a clean isolated worktree.
- **Outputs:** a test diff byte-equivalent to the candidate for the owned test, focused semantic proof, and a retained `/tmp` tee log recording the one mandatory `make validate` result.
- **Invariants:** no changes to `scripts/validate_ci_local.sh`, Dockerfiles, AGENTS, taskctl, lifecycle semantics, source, dependencies, MIP, GeoX, or capability authority; no duplicate Docker validation container.
- **Failure semantics:** a nonzero gate produces `blocked` with the retained failing tail, exact command/exit status, and a successor selected only from that evidence. Do not retry the full gate or repair an unrelated failure under this authority.

## Exact implementation behavior

1. Reproduce only the `a26f449` diff in `tests/test_repo_native_execution_handoff.py`; prove it equals that candidate diff for the owned path.
2. Run `python3 -m pytest tests/test_repo_native_execution_handoff.py -q`, JSON/taskctl/diff checks, then exactly one `make validate` through `tee` with `pipefail`, retaining the host log below `/tmp` until its final status and failing tail are recorded.
3. On pass, publish an exact-tree `ready_for_review` receipt. On failure, publish `blocked`; do not change a failing test or validation code.

## Owned paths

1. `tests/test_repo_native_execution_handoff.py`
2. `docs/execution/ACTIVE_TASK.md`
3. `docs/execution/EXECUTION_STATE.json`
4. `docs/execution/LATEST_COMPLETION_REPORT.md`

No other path is owned.

## Prohibited scope

Do not change Finder metadata, `.gitignore`, AGENTS, taskctl, validation scripts,
Docker configuration, source, fixtures, packages, APIs, schemas, contracts,
dependencies, CI, documentation outside execution files, MIP, GeoX, analytical,
public, runtime, or consumer behavior. Do not create a PR, merge, squash,
rebase, force-push, merge commit, or pre-merge approval commit.

## Acceptance evidence and validation

Required: exact candidate-diff proof; focused semantic test; JSON/taskctl/diff
checks; changed-path and remote-head proof; a single Docker-backed `make validate`
with retained tee output. Ruff and mypy are `not_required` separately but not
waived from `make validate`. A required failure is `blocked`.

## Authorization, publication, and stop conditions

This is proposed authoring only. Only a main-only `proposed -> authorized`
transition may create the declared branch. Publish a remote `ready_for_review`
only with a passing exact-tree gate, or a remote `blocked` receipt with retained
failure evidence. All merge, PR, correction, MIP, GeoX, analytical, sibling, and
capability authority remains false.

## Deferred successors

- Repair only the exact full-gate failure identified by this task under separate authority.
- Resume the assertion-repair and taskctl-fixture tasks only after their required gates pass and merge.
- Resume Finder hygiene only after those predecessors merge and its own full gate passes.

**Unresolved execution-blocking design questions: none.**
